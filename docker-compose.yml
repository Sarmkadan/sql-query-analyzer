services:
  sql-query-analyzer:
    build: .
    environment:
      - DB_SERVER=localhost
      - DB_PORT=1433
      - DB_USER=sa
      - DB_PASSWORD=YourPassword123!
      - DB_NAME=QueryAnalyzer
      - ANALYZER_LOG_LEVEL=Information
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
