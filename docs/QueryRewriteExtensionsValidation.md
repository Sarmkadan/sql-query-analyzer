# QueryRewriteExtensionsValidation

Provides validation utilities for query rewrite operations in SQL query analysis scenarios. This class contains methods to validate the correctness, applicability, and impact of query rewrites, along with summary generation and validation state checks.

## API

### `Validate`
