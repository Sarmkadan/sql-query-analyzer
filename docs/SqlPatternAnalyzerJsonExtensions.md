# SqlPatternAnalyzerJsonExtensions

Provides JSON serialization and deserialization utilities for SQL pattern analysis results and queries. The type offers methods to convert analysis results to JSON strings and to reconstruct objects or lists from JSON input, with both strict and tolerant parsing options.

## API

### `ToJson`
