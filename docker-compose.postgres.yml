version: '3.8'

services:
  sql-query-analyzer:
    environment:
      - DB_SERVER=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=YourPassword123!
      - DB_NAME=QueryAnalyzer

  postgres:
    extends:
      file: docker-compose.yml
      service: postgres
